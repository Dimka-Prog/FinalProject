{% extends 'base.html.twig' %}

{% block title %}Rooms{% endblock %}

{% block body %}
    <h1>Rooms</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ room.id }}</td>
            </tr>
            <tr>
                <th>RoomNum</th>
                <td>{{ room.RoomNum }}</td>
            </tr>
            <tr>
                <th>Places</th>
                <td>{{ room.Places }}</td>
            </tr>
            <tr>
                <th>Floor</th>
                <td>{{ room.Floor }}</td>
            </tr>
            <tr>
                <th>RoomStatus</th>
                <td>{{ room.RoomStatus }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rooms_index') }}">back to list</a>

    <a href="{{ path('app_rooms_edit', {'id': room.id}) }}">edit</a>

    {{ include('rooms/_delete_form.html.twig') }}
{% endblock %}

<form method="post" action="{{ path('app_settling_room_delete', {'id': settling_room.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ settling_room.id) }}">
    <button class="btn">Delete</button>
</form>
